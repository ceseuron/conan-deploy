- name: Create ConanSandbox mod temp directory.
  file:
    path: "/home/{{ conan_user }}/exiles/modtmp"
    state: directory
    owner: "{{ conan_user }}"
    group: "{{ conan_group }}"

- name: Download mods from Steam workshop.
  command: "/usr/games/steamcmd +@sSteamCmdForcePlatformType windows +login anonymous +force_install_dir /home/{{ conan_user }}/exiles/modtmp +workshop_download_item 443030 {{ item }}"
  become: yes
  become_user: "{{ conan_user }}"
